interface:
  display_name: "Browser Use"
  short_description: "Repo-specific patterns and workflows for browser-use"
  default_prompt: "Use the browser-use repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true